---
const { headText, bodyText, footText } = Astro.props
const { classAttr = "border"} = Astro.props;
---

<div class="p-5 m-5  bg-gray-400">
    <h1>{headText}</h1>
    <p>{bodyText}</p>
    <p>{footText}</p>
</div>