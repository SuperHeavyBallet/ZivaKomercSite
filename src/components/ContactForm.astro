---
const { headText, bodyText, footText } = Astro.props
---

<div class="p-5 m-5 border-2 border-solid bg-amber-300" id="contactForm">
    <h1>{headText}</h1>
    <p>{bodyText}</p>
    <p>{footText}</p>
</div>